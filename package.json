{
  "name": "judescode",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.14.0",
  "scripts": {
    "build": "pnpm --filter=@workspace/api-server build && pnpm --filter=@workspace/client build"
  },
  "engines": {
    "node": ">=18"
  }
}
